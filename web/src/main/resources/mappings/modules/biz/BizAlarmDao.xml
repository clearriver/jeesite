<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeesite.modules.biz.dao.BizAlarmDao">
	
	<!-- 查询数据
	<select id="findList" resultType="BizPlace">
		SELECT ${sqlMap.column.toSql()}
		FROM ${sqlMap.table.toSql()}
		<where>
			${sqlMap.where.toSql()}
		</where>
		ORDER BY ${sqlMap.order.toSql()}
	</select> -->
	
	<select id="queryMap" resultType="java.util.HashMap" parameterType="hashmap">
		SELECT a.alarm_code alarmCode,a.place_code placeCode,a.alarm_type alarmType,a.alarm_time alarmTime,a.deal_way dealWay,a.oos_url oosUrl,a.sign sign,p.place_name placeName  
		FROM ${_prefix}biz_alarm a,${_prefix}biz_place p
		<where>
			a.place_code=p.place_code ${andsql} 
		</where>
		ORDER BY a.place_code
	</select>
</mapper>